# ✦ Echo — Agentic Career Intelligence
### India Fresher Edition · 2025 · 100% Free · Fully Local

> Echo is your personal AI career agent. It scrapes jobs, matches them to your resume using local Ollama embeddings, finds skill gaps, generates LinkedIn posts, and chats with you — all running entirely on your machine. No paid APIs, no data leaks.

---

## 🖥️ Screenshots

Open `http://localhost:5173` after starting Echo. You'll see:
- **Chat panel** — talk to Echo in natural language
- **Job Matches** — ranked, filterable job cards with match scores  
- **LinkedIn Posts** — 6 post types generated by Ollama
- **Resume** — drag-drop upload with AI skill extraction
- **Settings** — all preferences editable from UI (saved to DB, no hardcoding)

---

## ⚡ Quick Start (3 steps)

### 1. Install Ollama
```bash
# Linux
curl -fsSL https://ollama.ai/install.sh | sh && ollama serve &
ollama pull llama3.2          # Main chat/reasoning model (~2GB)
ollama pull nomic-embed-text  # Embedding model (~270MB)

# Mac
brew install ollama && ollama serve &
ollama pull llama3.2
ollama pull nomic-embed-text
```

### 2. Install Echo
```bash
git clone <repo> echo_career_ai
cd echo_career_ai
pip install -r requirements.txt
cp .env.example .env          # Already has good defaults
python run.py setup           # Verify everything works
```

### 3. Start
```bash
python run.py all             # Starts API + Web UI together
```
Open **http://localhost:5173** → upload your resume → click "Run Agent" → done!

---

## 📁 Project Structure

```
echo_career_ai/
│
├── run.py                          ← Single entry point
├── .env                            ← All config (gitignored)
├── .env.example                    ← Template (safe to commit)
├── requirements.txt
├── .gitignore
│
├── config/
│   ├── settings.py                 ← Pydantic settings from .env
│   └── claude_desktop_mcp.json     ← MCP config for Claude Desktop
│
├── src/
│   ├── agent/
│   │   ├── brain/
│   │   │   ├── ollama_client.py    ← Async Ollama (chat, embed, stream)
│   │   │   └── logger.py
│   │   ├── memory/
│   │   │   ├── database.py         ← SQLite (8 tables, full schema)
│   │   │   └── vector_store.py     ← FAISS + Ollama embeddings
│   │   ├── scrapers/
│   │   │   └── job_scraper.py      ← Naukri, Indeed, Internshala, Wellfound
│   │   └── tools/
│   │       ├── resume_analyzer.py  ← LLM-powered extraction
│   │       ├── job_matcher.py      ← Embed → FAISS → LLM rerank
│   │       └── linkedin_generator.py
│   ├── api/
│   │   └── main.py                 ← FastAPI (REST + SSE streaming)
│   └── mcp/
│       └── server.py               ← MCP server (Claude Desktop)
│
├── web/
│   └── index.html                  ← Single-file Web UI (warm peach/mint theme)
│
└── data/                           ← Auto-created, gitignored
    ├── uploads/                    ← Resume files
    ├── reports/                    ← Daily reports
    ├── logs/                       ← Agent logs
    └── vector_store/               ← FAISS index files
```

---

## 💬 Chat Commands (natural language)

Echo understands intent — just type naturally:

```
show me my top jobs
why did job 42 match?
what skills am I missing?
generate a LinkedIn post for data analyst role
write a skill spotlight post about LangChain
start scraping jobs now
focus on remote jobs only
change my target role to AI Engineer
pause the agent
show stats
generate a week's worth of posts
```

---

## 🔌 MCP Integration (Claude Desktop)

Add to Claude Desktop's `claude_desktop_config.json`:

```json
{
  "mcpServers": {
    "echo": {
      "command": "python",
      "args": ["run.py", "mcp"],
      "cwd": "/full/path/to/echo_career_ai"
    }
  }
}
```

Then in Claude Desktop you can say: *"Use Echo to show my top job matches"*

**Available MCP tools:**
- `get_top_jobs` — top matched jobs with filters
- `explain_job_match` — why a job matched
- `get_skill_gaps` — skill gap analysis
- `generate_linkedin_post` — post generation
- `scrape_jobs` — trigger scraping
- `run_matching` — run matching pipeline
- `get_agent_stats` — statistics
- `get_resume` — current profile
- `set_preference` — update settings

---

## ⚙️ Configuration
Everything is configured via environment variables. See `.env.example` for all available options and defaults.

## 🏗️ How Matching Works

```
Your Resume PDF
      ↓
  Text Extract (pypdf)
      ↓
  Ollama LLM → Structured Profile (skills, roles, education)
      ↓
  nomic-embed-text → 768-dim vector → FAISS index
                                           ↑
Job Descriptions → Ollama embed → FAISS ──┘
                                           ↓
                              Cosine Similarity (0-1)
                                           ↓
                    score < 0.50  →  Skip
                    score 0.50-0.65 →  Ollama re-rank
                    score > 0.65  →  Save match
                                           ↓
                    match_reasons + skill_overlap + skill_gaps
```

---

## 🔒 Privacy

- ✅ All AI runs locally via Ollama
- ✅ Resume never leaves your machine
- ✅ No accounts, no API keys required
- ✅ Read-only scraping — never auto-applies to jobs
- ✅ SQLite + FAISS — all data stored locally

---

## 🚀 Roadmap

- [ ] Playwright-based scraping for JS-heavy sites
- [ ] Email digest reports
- [ ] Cover letter generator
- [ ] Interview prep Q&A
- [ ] Telegram bot interface
